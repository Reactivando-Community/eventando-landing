export const metadata = {
  title: "Analytics Dashboard | techstars_ Startup Weekend Anápolis",
  description:
    "Painel consolidado de analytics do Firebase para o Startup Weekend Anápolis — eventos, conversões e métricas de engajamento.",
  robots: { index: false, follow: false },
};

export default function AnalyticsLayout({ children }) {
  return <>{children}</>;
}
